import React from 'react';
import styled, { keyframes } from 'styled-components';
import LoginTemplate from '../Account/SignInUpSchema';

const Login = () => {
  return <LoginTemplate title="Login" />;
};

export default Login;
